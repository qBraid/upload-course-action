{
  "name": "upload-to-gcs-action",
  "version": "1.0.0",
  "description": "GitHub Action to upload repository contents to Google Cloud Storage",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build src/index.js -o dist --source-map --license licenses.txt",
    "test": "jest",
    "lint": "eslint src/**/*.js"
  },
  "keywords": [
    "github-action",
    "gcs",
    "upload",
    "google-cloud-storage"
  ],
  "author": "qBraid",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@google-cloud/storage": "^7.7.0",
    "glob": "^10.3.10"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.1",
    "eslint": "^8.56.0",
    "jest": "^29.7.0"
  }
}
